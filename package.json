{
  "name": "gameearn",
  "version": "2.0.0",
  "private": true,
  "description": "GameEarn — static discovery site with Supabase-backed games catalog and admin panel.",
  "scripts": {
    "build": "node build/generate-assets.js && node build/build.js",
    "generate:assets": "node build/generate-assets.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "engines": {
    "node": ">=18"
  }
}
